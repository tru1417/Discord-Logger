## Packages
framer-motion | Smooth animations for lists and page transitions
recharts | Analytics charts for server activity stats
date-fns | Formatting timestamps (e.g., "Today at 4:20 PM")
clsx | Utility for conditional classes
tailwind-merge | Utility for merging tailwind classes

## Notes
The dashboard is read-heavy (logs/cases).
Uses a dark Discord-inspired theme.
Navigation structure mimics a Discord client (sidebar + main view).
